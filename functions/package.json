{
  "name": "houseoffigs-functions",
  "description": "Firebase Cloud Functions for House of Figs — sends email notifications to admins on new quiz leads and intake submissions.",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo 'no lint configured' && exit 0",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "nodemailer": "^6.9.15"
  }
}
